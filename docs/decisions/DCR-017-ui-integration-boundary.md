# DCR-017：桌面 UI 与 sing-box 集成的开发和验收边界

用户决定：2026-09-05 明确要求重新梳理当前任务，不重复验证 sing-box 范围的能力；
核心是为 sing-box 增加 UI，参考本地 Clash Verge Rev，并按此规范开发后续功能。
该明确指令已授权本文件描述的需求/验收范围调整，不再等待同一方向的批准。
技术一致性审阅与冻结身份由 `.sdlc/state.yaml` 及对应 review Evidence 记录。

## 问题、现有设计与影响

TASK-009 原 SF-003 将 WireGuard/DNS/IPv6/Host/SNI 等内核行为矩阵列为前置验收，
DCR-009 至 DCR-015 逐步增加自建 peer、网络栈及报文级观察；DCR-016 又提出 IPv6 扩展。
这些完成条件超出当前产品责任，拖延 UI、配置与运行管理交付。

变更涉及 `docs/veyra.md` §1、§48、§143，根目录 `AGENTS.md`，TASK-009、滚动计划、
Anchor、Design/Delivery Gate 与恢复摘要。实现源码、既有测试、依赖、配置资产、权限、
平台和运行资源均不在本次文档重整的修改范围内。

本决定替代 DCR-003“TASK-009 必须完成全部受控 WG/DNS 运行矩阵”的完成条件，以及
DCR-009/010/011/012/013/014/015 中重复要求继续补全同一内核矩阵的完成条件；
DCR-016 撤下，不实施，不再等待 IPv6 资源批准。原文和当时审批/审阅按原身份保留。
各旧 DCR 的已实现配置、安全和运行行为没有因此被删除或放宽。

## 确认的职责分工

| Veyra 必须交付并验收 | sing-box 上游负责，Veyra 不重复认证 |
| --- | --- |
| 页面布局、输入、空态/加载/忙碌/错误、用户操作反馈 | TCP/UDP、WireGuard、TLS 等协议实现 |
| 订阅归一化、领域状态与持久化、配置字段映射及最终 check | IPv4/IPv6 转发、校验和、重传、TCP ACK |
| 同一配置的启动/Ready/停止、失败传播与资源所有权 | DNS resolver、URLTest 探测及 Host/SNI 的内部算法 |
| 固定鉴权 API、脱敏、安全 DTO、实例隔离、流量/日志展示 | 上游内核完整协议/地址族兼容认证矩阵 |
| 应用生成拒绝规则与优先级、私有配置/secret、平台补偿 | 通过自建网络栈重做包级正确性测试 |

保留 Tauri/React/Rust、领域模型、独立 Compiler 与固定 sing-box sidecar；只改变交付
优先级和验证责任，不重构现有架构。新增测试必须先指出所保护的 Veyra 自有用户行为或
契约。最小真实集成是 config/check/run/API/stop，必要时复用一个已有受控连通/计量场景。
既有代表性拒绝用例可支持应用策略接线；不要求扩展地址族/协议/目标的排列组合。
涉及具体可复现产品缺陷时允许定向诊断，不把诊断扩展成通用协议认证设施。

## UI 参考与后续顺序

参考仓库：`E:/wx_lifeilin/github.com/clash-verge-rev`，本轮只读观察 HEAD
`ff4b4dd8229ebf3de630c141dff189b5ed5bb7fb`，不是锁死后续参考版本。
CodeGraph 读回 `_navigation.tsx`、`_routers.tsx`、layout 与 `layout-traffic.tsx`，确认
导航分页面及侧栏流量组件；没有运行该项目，也不宣称已完成视觉/交互验收。

- 首页/侧栏、运行状态、实时网速/累计与趋势、窗口/托盘体验：先完成当前已实现表面及
  sing-box 接线，再进入桌面联合 UI Task。
- 订阅页：导入、手动更新、失败保留旧节点、更新时间及用户反馈。
- 代理/出口组与规则页：映射 Veyra 的节点/Pool、选择、默认出口及分流编辑。
- 连接、日志、设置页：展示 sing-box 数据和应用自有设置，按当前能力提供明确空态与错误态。

每项 UI 实现先定位参考页面/组件，记录与 Veyra 的映射、交互和验收；不是复制参考项目
Mihomo JSON/API、脚本、依赖或默认加入其全部功能。已有捕获模式、Native/Tor、平台和
权限 Gate 仍适用；不在本次重整中开放新 listener、TUN/System Proxy 或生产操作。

## 当前任务与旧证据的处理

TASK-009 收敛到三个可交付子功能：SF-001 应用配置事务/受管生命周期；SF-002 现有
首页/Start/Stop/Toast 与观测展示；SF-003 最小真实内核集成及应用自有配置安全边界。
TASK-010 保留桌面导航/布局与 GUI/Tray 联合路径，TASK-011 保留订阅用户流程；
其它页面只做未来轻量规划，不绕过当前 Task 的独立验收提前实现。

现有测试和 checkpoint025 原始成功/失败证据全部保留，不删除/禁用测试、不抑制 lint，
也不把旧结果重新归属到新验收身份。停止新增协议设施；本次不触碰那些既有测试文件。
后续改动若影响 Veyra 自有逻辑或共享测试资源的正确性，只做明确影响所需的定向回归。

HTTP025 DnsError 和 reject025 socket-query timeout 仍是未闭合历史 FAIL，不宣称修复。
不因它们来自协议测试就推断 sing-box 有错或 Veyra 无错；先检查已有失败摘要是否涉及
应用自有配置/进程/资源路径。没有具体 Veyra 缺陷证据时不将整个旧协议矩阵继续当作 Gate。
有自有路径问题则记录最小问题并处理，不能借收敛范围屏蔽安全/清理失败。

原整任务交付审阅基于旧 Acceptance，对新的整体验收为 STALE；代码分区结论和测试结果
仍只对原身份有效，可逐项对账，不自动通过新 Gate。Task 保持 IN_PROGRESS、验收 PENDING；
新范围需要自己的交付对账/必要补证和独立交付审阅，之后才请求人工 Task 验收。

## 验证、风险与兼容

本次只校验规范/需求/Task/DCR 的一致性、引用、身份、计划依赖与 `git diff --check`，
由未参与编写的 Reviewer 独立审阅；不执行内核、网络、构建或协议测试。
本次降低的是重复内核认证覆盖，未取得的内核矩阵证据不再作为产品承诺；保留 Veyra 的
正反配置检查、运行失败矩阵、鉴权/脱敏/权限和用户可见交互验证，控制实际产品风险。
无 migration、兼容层、fallback、运行依赖或系统配置变化，无 commit/push/发布授权。

权威关系：当前用户决定 → 需求 §1.2/§143.1 与本 DCR → 当前 Task/根目录开发规则 →
state 中的身份及 Gate → HANDOFF。旧工件仅在本文件明确替代的验证义务上停止作为前置；
其它产品、安全与平台约束继续生效。
