# TASK-011 subscription policy 025

Status: CANDIDATE. Authority: USER response to decision024 explicitly chooses merge identical nodes, retain first; Clash certificate pins must error; use sing-box UA. This supersedes only import duplicate handling of prior design. Current UI021 overrides only presentation of skipped counts, not persisted count or smux policy.

1. Existing representable semantics only: empty VLESS flow -> None; explicit xudp -> existing default (reject other explicit encodings); HY2 udp:true -> existing default; false/invalid remain rejected. Only selected sing-box outbounds context omits direct/block/dns/selector/urltest as configuration/group metadata. No routing import. Proxies context retains strict node treatment. Unknown real protocols/fields still fail atomically.
2. Application parse_content accepts no invalid/unknown nodes and at least one supported node under existing smux rule, then merges exact complete typed connection tuples excluding name: (protocol, server, port, options, transport, TLS). Use full serialization equality in a HashSet, not 64-bit NodeId/hash equality. Keep first occurrence order/name and let unchanged normalize_nodes generate its same stable ID. All import/update/URL replacement callers share this boundary. Different credentials, TLS, transport, port, or options are never merged. Strict generic normalization/provider replacement are unchanged. Duplicates are not unsupported skips and do not increase skipped count. No persisted model/ID algorithm migration.
3. Clash proxies carrying a syntactically valid full certificate SHA256 fingerprint remain a whole-document refusal. Add closed safe code unsupportedCertificatePin through SubscriptionOperationError -> command SubscriptionErrorCode -> TypeScript whitelist/message. Return no URL/hash/node content. Message instructs selection of sing-box User-Agent. A malformed fingerprint still results in rejection; no pin dropped or mapped to public-key hash. Do not automatically fetch another UA. Check structure in existing source preflight; sing-box JSON parsing must not break on Unicode surrogate escapes.
4. Failure preserves prior state/cache/nodes and reference/runtime behavior; changing import normalization does not add automatic activation. No network node execution. sing-box response is imported as server produced it; no claim it is semantically equivalent to the Clash response, which includes certificate pin and hopping settings omitted by the server's sing-box output.
5. Verification: actual-shape safe VLESS emptyflow/xudp and groups; all-invalid/all-metadata rejection; exact duplicates with different names merge first, distinct connection options remain; stable first ID and idempotent updates; mixed invalid content retains exact state; pin rejection safe serialized DTO + UI message; source freshness. Real response diagnostic outputs counts only and any body is memory-only. No sing-box protocol tests, no dependencies/permissions/resource modifications.

Production owned scope: parser.rs; subscription_management.rs; commands.rs; src/lib/subscriptions.ts and .test.ts; user-approved UI SubscriptionPage.tsx/styles. Tests/fixtures and evidence only outside these. Same TASK011 baseline001/HEAD506553d2 remains. UI work proceeds independently; policy implementation awaits independent design review. User decision is already approved; do not re-ask.

References: https://wiki.metacubex.one/config/proxies/tls/#fingerprint ; https://sing-box.sagernet.org/configuration/shared/tls/#certificate_public_key_sha256 ; https://sing-box.sagernet.org/configuration/outbound/vless/#packet_encoding .
