# TASK-011 partial node import policy 030

Status: CANDIDATE. User explicitly supersedes policy025 whole-document certificate-pin refusal: import Clash or sing-box supported nodes and filter unsupported entries. User also requests compact used/total card and asks whether SF-Pro.ttf is the reference UI font. TASK011 remains current; baseline001 unchanged.

1. The existing parser remains the authority for whether a complete node is representable. Do not strip certificate pins, unknown TLS settings, credentials or transport fields to make an otherwise unsupported node pass. Each rejected candidate is filtered as a whole and contributes to the existing persisted skipped-node count. This covers existing UnsupportedProtocol, UnsupportedOption and InvalidNode classifications (unknown unsupported fields are currently classified InvalidNode). Successfully parsed nodes retain their full typed parameters. Whole-document syntax/format/fetch failures still fail; a zero-supported-node result fails and preserves the previous state/cache, rather than emptying an existing subscription.
2. Remove the application-wide certificate-pin preflight refusal. The unchanged parser already rejects fingerprint-bearing nodes; import remaining nodes and show a generic result warning count. Remove the now-unreachable UnsupportedCertificatePin enum/DTO/TS message and replace its regression with filtered-import behavior. Keep provider-only Clash-document error, source size limits, strict generic normalization/provider replacement and full-tuple dedup retaining first. Duplicate merges are not unsupported skips. Unified application parse_content applies the same per-node filtering across its supported input formats, so the input wrapper does not change rejection semantics.
3. Import, manual/remote update and URL replacement use the same filtering boundary; persist filtered count with resulting nodes atomically. 304 preserves the count. All-filtered, document failure, fetch failure and save failure keep the prior committed state. No automatic activation or UA switch is added; default Clash UA and explicit sing-box choice are unchanged.
4. UI result wording is '已过滤 N 个不支持的节点'; do not show this on subscription cards. Existing skipped-count storage and safe DTO fields are reused, no schema/dependency/permission change. Card displays compact used / total beside expiry and a thin progress bar, without remaining/used/total repeated labels. Empty metadata retains equal card height. Root verifies actual reference font loading; do not infer usage solely from bundled SF-Pro.ttf presence.
5. Verify mixed supported/pinned/unknown/invalid nodes retain supported values and count every filtered node, exact duplicates merge first, all-filtered import/update preserve saved bytes, 304 maintains filtered count, safe DTO/message and compact card light/dark widths. Use synthetic fixtures and optional previously authorized memory-only response diagnostics; no node execution or sing-box conformance tests.

Owned production scope: subscription_management.rs, commands.rs, src/lib/subscriptions.ts, SubscriptionPage.tsx/styles.css. Directly related tests/evidence only. Parser production logic is unchanged, retaining its fail-closed per-node decisions. Existing UI and non-scope dirty work preserved. Independent design review precedes policy implementation; current user request is authorization, no repeated approval.
