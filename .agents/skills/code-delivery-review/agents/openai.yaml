interface:
  display_name: "Code Delivery Review"
  short_description: "独立全面审查冻结交付单元的覆盖、身份、证据和实现风险"
  default_prompt: "使用 $code-delivery-review 对当前冻结 Task 执行完整覆盖、身份新鲜且只读的独立交付审查。"

policy:
  allow_implicit_invocation: true
